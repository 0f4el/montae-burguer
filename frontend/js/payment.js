// Fecha a tela exibida durante o redirecionamento ao checkout da InfinitePay.
function fecharModalPix() {
    const modal = document.getElementById('pix-modal');
    if (modal) modal.classList.add('hidden');
}
